[user]
	name = <%= print("Your Name: "); STDOUT.flush; STDIN.gets.chomp %>
	email = <%= print("Your Email: "); STDOUT.flush; STDIN.gets.chomp %>
[color]
	diff = auto
	status = auto
  branch = auto
  ui = auto
[pager]
  diff =
[merge]
  tool= vimdiff
[core]
	excludesfile = <%= ENV['HOME'] %>/.gitignore
	editor = vim
  autocrlf = input
  pager = less -FXR
[apply]
	whitespace = nowarn
[format]
	pretty = %C(yellow)%h%Creset %s %C(red)(%an, %cr)%Creset
[github]
	user = <%= print("GitHub Username: "); STDOUT.flush; STDIN.gets.chomp %>
[alias]
  cdiff = diff --color=always
  ca = commit -a
  cav = commit -av
  st = status
  br = branch
  co = checkout
  lol = log --graph --decorate --pretty=oneline --abbrev-commit
  lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
  lolh = log --graph --decorate --pretty=oneline --abbrev-commit -n 13
  lolah = log --graph --decorate --pretty=oneline --abbrev-commit --all -n 13
  ls = ls-files
  p4m = mergetool --tool=p4merge
[color "diff"]
  new = blue
  old = red bold
