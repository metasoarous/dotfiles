
options(width=150)
options(editor='vim')

options(menu.graphics=FALSE)

options(repos=structure(c(CRAN="http://cran.fhcrc.org")))


fill_screen <- function(how_wide=Sys.getenv("COLUMNS")) {
  options(width=as.integer(how_wide))
}

